---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import OceanBackground from '../components/OceanBackground.astro';
import CopyrightBar from '../components/CopyrightBar.astro';
import etapesData from '../data/terrain-etapes.json';
---

<BaseLayout
  title="Admin Terrain | OceanPhenix"
  description="Interface d'administration du parcours terrain"
  lastModified="12/03/2026 12:00"
>
  <link slot="head" rel="stylesheet" href="/lib/leaflet.min.css" />
  <OceanBackground />
  <Nav />

  <main class="main-content admin-page">

    <!-- Auth gate -->
    <div id="admin-auth" class="admin-auth">
      <div class="admin-auth-card">
        <h1><i class="fas fa-lock"></i> Admin Terrain</h1>
        <p>AccÃ¨s rÃ©servÃ© â€” entrez le mot de passe d'administration.</p>
        <div class="admin-form-group">
          <input type="password" id="admin-pass" placeholder="Mot de passe" autocomplete="off" />
        </div>
        <div class="admin-form-group admin-captcha-group">
          <label id="captcha-label" class="captcha-question"></label>
          <input type="text" id="admin-captcha" placeholder="Votre rÃ©ponse" autocomplete="off" inputmode="numeric" />
        </div>
        <button id="admin-login-btn" class="admin-btn admin-btn--primary">
          <i class="fas fa-sign-in-alt"></i> AccÃ©der
        </button>
        <p id="admin-auth-error" class="admin-error" style="display:none;">Mot de passe ou captcha incorrect</p>
      </div>
    </div>

    <!-- Admin panel (hidden until auth) -->
    <div id="admin-panel" class="admin-panel" style="display:none;">

      <!-- Top bar -->
      <div class="admin-topbar">
        <h1><i class="fas fa-cogs"></i> Admin â€” Terrain Data</h1>
        <div class="admin-topbar-actions">
          <button id="admin-save-btn" class="admin-btn admin-btn--primary">
            <i class="fas fa-save"></i> Sauvegarder (localStorage)
          </button>
          <button id="admin-export-btn" class="admin-btn admin-btn--secondary">
            <i class="fas fa-download"></i> Exporter JSON
          </button>
          <button id="admin-import-btn" class="admin-btn admin-btn--outline">
            <i class="fas fa-upload"></i> Importer JSON
          </button>
          <input type="file" id="admin-import-file" accept=".json" style="display:none" />
        </div>
      </div>

      <!-- Tabs -->
      <div class="admin-tabs">
        <button class="admin-tab admin-tab--active" data-tab="dashboard">
          <i class="fas fa-tachometer-alt"></i> Dashboard
        </button>
        <button class="admin-tab" data-tab="etapes">
          <i class="fas fa-map-marked-alt"></i> Ã‰tapes
        </button>
        <button class="admin-tab" data-tab="journal">
          <i class="fas fa-book-open"></i> Journal
        </button>
        <button class="admin-tab" data-tab="photos">
          <i class="fas fa-images"></i> Photos
        </button>
        <button class="admin-tab" data-tab="gpx">
          <i class="fas fa-route"></i> Parcours GPX
        </button>
        <button class="admin-tab" data-tab="preview">
          <i class="fas fa-eye"></i> AperÃ§u
        </button>
        <button class="admin-tab" data-tab="villes">
          <i class="fas fa-map-pin"></i> Villes France
        </button>
        <button class="admin-tab" data-tab="coworking">
          <i class="fas fa-building"></i> Coworking
        </button>
      </div>

      <!-- ============================== -->
      <!-- TAB: Dashboard                 -->
      <!-- ============================== -->
      <div class="admin-tab-content admin-tab-content--active" id="tab-dashboard">
        <div class="admin-card">
          <h2><i class="fas fa-tachometer-alt"></i> DonnÃ©es du dashboard</h2>
          <div class="admin-form-row">
            <div class="admin-form-group">
              <label>Km parcourus</label>
              <input type="number" id="dash-km" min="0" step="1" />
            </div>
            <div class="admin-form-group">
              <label>Jours de route</label>
              <input type="number" id="dash-jours" min="0" step="1" />
            </div>
          </div>
          <div class="admin-form-row">
            <div class="admin-form-group">
              <label>Besoins identifiÃ©s</label>
              <input type="number" id="dash-besoins" min="0" step="1" />
            </div>
            <div class="admin-form-group">
              <label>Rencontres entreprises</label>
              <input type="number" id="dash-rencontres" min="0" step="1" />
            </div>
          </div>
          <div class="admin-form-row">
            <div class="admin-form-group">
              <label>Ã‰tape en cours (texte libre)</label>
              <input type="text" id="dash-encours" placeholder="Ex: En route vers Dreux" />
            </div>
            <div class="admin-form-group">
              <label>Prochaine Ã©tape</label>
              <select id="dash-prochaine"></select>
            </div>
          </div>
          <div class="admin-form-row">
            <div class="admin-form-group">
              <label>Jours prÃ©vus</label>
              <input type="number" id="dash-joursprevus" min="0" step="1" />
            </div>
          </div>
        </div>

        <div class="admin-card">
          <h2><i class="fas fa-map-pin"></i> Position actuelle</h2>
          <div class="admin-form-row">
            <div class="admin-form-group">
              <label>Ville</label>
              <input type="text" id="pos-ville" />
            </div>
            <div class="admin-form-group">
              <label>Statut</label>
              <select id="pos-statut">
                <option value="PrÃ©paration">PrÃ©paration</option>
                <option value="En route">En route</option>
                <option value="Ã‰tape">Ã‰tape</option>
                <option value="Repos">Repos</option>
                <option value="TerminÃ©">TerminÃ©</option>
              </select>
            </div>
          </div>
          <div class="admin-form-row">
            <div class="admin-form-group">
              <label>Latitude</label>
              <input type="number" id="pos-lat" step="0.0001" />
            </div>
            <div class="admin-form-group">
              <label>Longitude</label>
              <input type="number" id="pos-lng" step="0.0001" />
            </div>
          </div>
          <div class="admin-form-group">
            <label>Date de dÃ©part</label>
            <input type="date" id="pos-depart" />
          </div>
        </div>
      </div>

      <!-- ============================== -->
      <!-- TAB: Ã‰tapes                    -->
      <!-- ============================== -->
      <div class="admin-tab-content" id="tab-etapes">
        <div class="admin-card">
          <h2><i class="fas fa-map-marked-alt"></i> Gestion des Ã©tapes</h2>
          <p class="admin-hint">Cliquez sur une Ã©tape pour la modifier. Changez le statut pour marquer une Ã©tape comme visitÃ©e.</p>
          <div class="admin-form-actions" style="margin-bottom:1rem;">
            <button id="etape-add-btn" class="admin-btn admin-btn--primary">
              <i class="fas fa-plus"></i> Nouvelle Ã©tape
            </button>
          </div>
          <div style="overflow-x:auto;">
            <table id="etapes-table" class="admin-etapes-table">
              <thead>
                <tr>
                  <th style="width:40px;">#</th>
                  <th>Ville</th>
                  <th>RÃ©gion</th>
                  <th style="text-align:center;">Km</th>
                  <th style="text-align:center;">Statut</th>
                  <th style="text-align:center;">Lat</th>
                  <th style="text-align:center;">Lng</th>
                  <th style="text-align:center;width:80px;">Actions</th>
                </tr>
              </thead>
              <tbody id="etapes-list"></tbody>
            </table>
          </div>
          <div id="etape-editor" class="admin-etape-editor" style="display:none;">
            <h3 id="etape-editor-title">Modifier l'Ã©tape</h3>
            <input type="hidden" id="etape-edit-id" />
            <div class="admin-form-row">
              <div class="admin-form-group">
                <label>Ville</label>
                <input type="text" id="etape-edit-ville" />
              </div>
              <div class="admin-form-group">
                <label>RÃ©gion</label>
                <input type="text" id="etape-edit-region" />
              </div>
            </div>
            <div class="admin-form-row">
              <div class="admin-form-group">
                <label>Statut</label>
                <select id="etape-edit-statut">
                  <option value="planifie">PlanifiÃ©</option>
                  <option value="actuel">Actuel</option>
                  <option value="visite">VisitÃ©</option>
                </select>
              </div>
              <div class="admin-form-group">
                <label>Distance depuis dÃ©part (km)</label>
                <input type="number" id="etape-edit-dist" min="0" />
              </div>
            </div>
            <div class="admin-form-row">
              <div class="admin-form-group">
                <label>Latitude</label>
                <input type="number" id="etape-edit-lat" step="0.0001" />
              </div>
              <div class="admin-form-group">
                <label>Longitude</label>
                <input type="number" id="etape-edit-lng" step="0.0001" />
              </div>
            </div>
            <div class="admin-form-group">
              <label>Date estimÃ©e</label>
              <input type="date" id="etape-edit-date" />
            </div>
            <div class="admin-form-group">
              <label>Description</label>
              <textarea id="etape-edit-desc" rows="3"></textarea>
            </div>
            <div class="admin-form-actions">
              <button id="etape-save-btn" class="admin-btn admin-btn--primary">
                <i class="fas fa-check"></i> Valider
              </button>
              <button id="etape-cancel-btn" class="admin-btn admin-btn--outline">
                <i class="fas fa-times"></i> Annuler
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- ============================== -->
      <!-- TAB: Journal                   -->
      <!-- ============================== -->
      <div class="admin-tab-content" id="tab-journal">
        <div class="admin-card">
          <h2><i class="fas fa-book-open"></i> Journal du terrain</h2>
          <div id="journal-list" class="admin-journal-list"></div>
          <h3 class="admin-subtitle"><i class="fas fa-plus"></i> Nouvelle entrÃ©e</h3>
          <div class="admin-form-row">
            <div class="admin-form-group">
              <label>Date</label>
              <input type="date" id="journal-date" />
            </div>
            <div class="admin-form-group">
              <label>Ville</label>
              <input type="text" id="journal-ville" />
            </div>
          </div>
          <div class="admin-form-group">
            <label>Titre</label>
            <input type="text" id="journal-titre" placeholder="Ex: ArrivÃ©e Ã  Caen â€” premiers contacts" />
          </div>
          <div class="admin-form-group">
            <label>Contenu</label>
            <textarea id="journal-contenu" rows="5" placeholder="Racontez votre journÃ©e, rencontres, observations..."></textarea>
          </div>
          <div class="admin-form-group">
            <label>Tags (sÃ©parÃ©s par des virgules)</label>
            <input type="text" id="journal-tags" placeholder="rencontre, diagnostic, data, sponsor" />
          </div>
          <button id="journal-add-btn" class="admin-btn admin-btn--primary">
            <i class="fas fa-plus"></i> Ajouter au journal
          </button>
        </div>
      </div>

      <!-- ============================== -->
      <!-- TAB: Photos                    -->
      <!-- ============================== -->
      <div class="admin-tab-content" id="tab-photos">
        <div class="admin-card">
          <h2><i class="fas fa-images"></i> Photos du parcours</h2>
          <p class="admin-hint">
            Les photos sont rÃ©fÃ©rencÃ©es par leur chemin dans <code>/Images/terrain/</code>.
            Uploadez-les via FTP ou le gestionnaire de fichiers O2switch, puis ajoutez le chemin ici.
          </p>

          <div class="admin-photos-grid" id="photos-grid"></div>

          <h3 class="admin-subtitle"><i class="fas fa-plus"></i> Ajouter une photo</h3>
          <div class="admin-form-group">
            <label>SÃ©lectionner une image (pour prÃ©visualiser et obtenir le nom)</label>
            <input type="file" id="photo-file-input" accept="image/*" />
          </div>
          <div class="admin-photo-preview" id="photo-preview" style="display:none;">
            <img id="photo-preview-img" alt="AperÃ§u" />
            <div class="admin-photo-preview-info">
              <p>Fichier : <strong id="photo-preview-name"></strong></p>
              <p>Chemin qui sera ajoutÃ© : <code id="photo-preview-path"></code></p>
            </div>
          </div>
          <div class="admin-form-group">
            <label>Ou entrer le chemin manuellement</label>
            <input type="text" id="photo-path-input" placeholder="/Images/terrain/nom-du-fichier.jpg" />
          </div>
          <div class="admin-form-actions">
            <button id="photo-add-btn" class="admin-btn admin-btn--primary">
              <i class="fas fa-plus"></i> Ajouter la photo
            </button>
            <button id="photo-download-btn" class="admin-btn admin-btn--secondary" style="display:none;">
              <i class="fas fa-download"></i> TÃ©lÃ©charger (pour upload FTP)
            </button>
          </div>
        </div>
      </div>

      <!-- ============================== -->
      <!-- TAB: GPX                       -->
      <!-- ============================== -->
      <div class="admin-tab-content" id="tab-gpx">
        <div class="admin-card">
          <h2><i class="fas fa-route"></i> Parcours GPX</h2>
          <p class="admin-hint">
            Importez vos fichiers GPX Garmin / Strava / Komoot. Le tracÃ© sera affichÃ© sur la carte d'aperÃ§u
            et le fichier sera prÃªt Ã  copier dans <code>/public/gpx/</code>.
          </p>

          <div class="admin-form-group">
            <label>Charger un fichier GPX</label>
            <input type="file" id="gpx-file-input" accept=".gpx" />
          </div>

          <div id="gpx-info" class="admin-gpx-info" style="display:none;">
            <div class="admin-form-row">
              <div class="admin-form-group">
                <label>Nom du fichier</label>
                <input type="text" id="gpx-filename" readonly />
              </div>
              <div class="admin-form-group">
                <label>Points GPS</label>
                <input type="text" id="gpx-points" readonly />
              </div>
            </div>
            <div class="admin-form-row">
              <div class="admin-form-group">
                <label>Distance estimÃ©e</label>
                <input type="text" id="gpx-distance" readonly />
              </div>
              <div class="admin-form-group">
                <label>DÃ©nivelÃ© estimÃ©</label>
                <input type="text" id="gpx-elevation" readonly />
              </div>
            </div>
          </div>

          <div id="gpx-map" class="admin-gpx-map" style="display:none;"></div>

          <div id="gpx-actions" class="admin-form-actions" style="display:none;">
            <button id="gpx-download-btn" class="admin-btn admin-btn--secondary">
              <i class="fas fa-download"></i> TÃ©lÃ©charger le GPX
            </button>
            <button id="gpx-add-btn" class="admin-btn admin-btn--primary">
              <i class="fas fa-plus"></i> Ajouter au parcours
            </button>
          </div>

          <h3 class="admin-subtitle">Fichiers GPX enregistrÃ©s</h3>
          <div id="gpx-list" class="admin-gpx-list">
            <p class="admin-hint">Aucun fichier GPX enregistrÃ© pour le moment.</p>
          </div>
        </div>
      </div>

      <!-- ============================== -->
      <!-- TAB: Preview                   -->
      <!-- ============================== -->
      <div class="admin-tab-content" id="tab-preview">
        <div class="admin-card">
          <h2><i class="fas fa-eye"></i> AperÃ§u des donnÃ©es</h2>
          <p class="admin-hint">Voici le JSON complet tel qu'il sera exportÃ©. VÃ©rifiez les donnÃ©es avant de les exporter.</p>
          <pre id="preview-json" class="admin-json-preview"></pre>
          <div class="admin-form-actions">
            <button id="preview-copy-btn" class="admin-btn admin-btn--primary">
              <i class="fas fa-copy"></i> Copier le JSON
            </button>
            <button id="preview-export-btn" class="admin-btn admin-btn--secondary">
              <i class="fas fa-download"></i> Exporter en fichier
            </button>
          </div>
        </div>
      </div>

      <!-- ============================== -->
      <!-- TAB: Villes de France            -->
      <!-- ============================== -->
      <div class="admin-tab-content" id="tab-villes">
        <div class="admin-card">
          <h2><i class="fas fa-map-pin"></i> CoordonnÃ©es des grandes villes de France</h2>
          <p class="admin-hint">Recherchez une ville puis cliquez sur <strong>Copier</strong> pour rÃ©cupÃ©rer ses coordonnÃ©es.</p>
          <input type="text" id="villes-search" placeholder="Rechercher une villeâ€¦" style="width:100%;padding:10px 14px;margin-bottom:16px;border-radius:8px;border:1px solid #333;background:#181e26;color:#e0e0e0;font-size:1rem;" />
          <div style="overflow-x:auto;">
            <table id="villes-table" style="width:100%;border-collapse:collapse;font-size:0.95rem;">
              <thead>
                <tr style="background:#1a2030;">
                  <th style="padding:10px 12px;text-align:left;border-bottom:2px solid #4db8d4;color:#4db8d4;">Ville</th>
                  <th style="padding:10px 12px;text-align:left;border-bottom:2px solid #4db8d4;color:#4db8d4;">RÃ©gion</th>
                  <th style="padding:10px 12px;text-align:center;border-bottom:2px solid #4db8d4;color:#4db8d4;">Latitude</th>
                  <th style="padding:10px 12px;text-align:center;border-bottom:2px solid #4db8d4;color:#4db8d4;">Longitude</th>
                  <th style="padding:10px 12px;text-align:center;border-bottom:2px solid #4db8d4;color:#4db8d4;">Action</th>
                </tr>
              </thead>
              <tbody id="villes-tbody"></tbody>
            </table>
          </div>
          <p id="villes-count" style="margin-top:12px;color:#888;font-size:0.85rem;"></p>
        </div>
      </div>

      <!-- ============================== -->
      <!-- TAB: Coworking                  -->
      <!-- ============================== -->
      <div class="admin-tab-content" id="tab-coworking">
        <div class="admin-card">
          <h2><i class="fas fa-building"></i> Espaces de coworking</h2>
          <p class="admin-hint">GÃ©rez les espaces de coworking. Ils apparaissent comme <i class="fas fa-flag"></i> drapeaux sur la carte terrain.</p>
          <div class="admin-form-actions" style="margin-bottom:1rem;">
            <button id="cw-add-btn" class="admin-btn admin-btn--primary">
              <i class="fas fa-plus"></i> Nouveau coworking
            </button>
          </div>
          <div style="overflow-x:auto;">
            <table class="admin-etapes-table" id="cw-table">
              <thead>
                <tr>
                  <th style="width:35px;">#</th>
                  <th>Nom</th>
                  <th>Ville</th>
                  <th style="text-align:center;">Lat</th>
                  <th style="text-align:center;">Lng</th>
                  <th style="text-align:center;">Carte</th>
                  <th style="text-align:center;width:80px;">Actions</th>
                </tr>
              </thead>
              <tbody id="cw-tbody"></tbody>
            </table>
          </div>
          <p id="cw-count" style="margin-top:12px;color:#888;font-size:0.85rem;"></p>
          <div id="cw-editor" style="display:none;margin-top:1rem;" class="admin-etape-editor">
            <h3 id="cw-editor-title">Nouveau coworking</h3>
            <input type="hidden" id="cw-edit-idx" value="-1" />
            <div class="admin-form-row">
              <div class="admin-form-group">
                <label>Nom de l'espace</label>
                <input type="text" id="cw-edit-nom" placeholder="WeWork, La CordÃ©eâ€¦" />
              </div>
              <div class="admin-form-group">
                <label>Ville</label>
                <input type="text" id="cw-edit-ville" placeholder="Lyon, Parisâ€¦" />
              </div>
            </div>
            <div class="admin-form-row">
              <div class="admin-form-group">
                <label>Adresse</label>
                <input type="text" id="cw-edit-adresse" placeholder="12 Rue de la Paix" />
              </div>
              <div class="admin-form-group">
                <label>Site web</label>
                <input type="url" id="cw-edit-url" placeholder="https://â€¦" />
              </div>
            </div>
            <div class="admin-form-row">
              <div class="admin-form-group">
                <label>Latitude</label>
                <input type="number" id="cw-edit-lat" step="0.0001" />
              </div>
              <div class="admin-form-group">
                <label>Longitude</label>
                <input type="number" id="cw-edit-lng" step="0.0001" />
              </div>
            </div>
            <div class="admin-form-group">
              <label><input type="checkbox" id="cw-edit-visible" checked /> Afficher sur la carte terrain</label>
            </div>
            <div class="admin-form-actions">
              <button id="cw-save-btn" class="admin-btn admin-btn--primary">
                <i class="fas fa-check"></i> Valider
              </button>
              <button id="cw-cancel-btn" class="admin-btn admin-btn--outline">
                <i class="fas fa-times"></i> Annuler
              </button>
            </div>
          </div>
        </div>
      </div>

    </div>
  </main>

  <CopyrightBar />

  <!-- Leaflet JS (self-hosted) -->
  <script src="/lib/leaflet.min.js" is:inline></script>

  <!-- Data for admin scripts (CSP-safe: not executed) -->
  <script type="application/json" id="etapes-data" set:html={JSON.stringify(etapesData)}></script>

  <!-- Admin JS -->
  <script src="/js/admin-terrain.js" is:inline></script>
</BaseLayout>
<style>
  /* ============================== */
  /* ADMIN PAGE STYLES              */
  /* ============================== */

  .admin-page {
    position: relative;
    z-index: 1;
    padding-top: 5rem;
    min-height: 100vh;
  }

  /* Auth */
  .admin-auth {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 60vh;
    padding: 2rem;
  }

  .admin-auth-card {
    background: rgba(12, 20, 36, 0.92);
    border: 1px solid rgba(45, 130, 170, 0.15);
    border-radius: var(--radius-lg);
    padding: 2.5rem;
    text-align: center;
    max-width: 400px;
    width: 100%;
  }

  .admin-auth-card h1 {
    font-size: var(--text-xl);
    color: var(--white);
    margin-bottom: 0.5rem;
  }

  .admin-auth-card h1 i { color: var(--accent-cyan); }

  .admin-auth-card p {
    color: var(--text-muted);
    font-size: var(--text-sm);
    margin-bottom: 1.5rem;
  }

  .admin-error {
    color: #f87171;
    font-size: var(--text-sm);
    margin-top: 0.75rem;
  }

  .admin-captcha-group {
    margin-top: 0.25rem;
  }

  .captcha-question {
    display: block;
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--accent-cyan);
    margin-bottom: 0.35rem;
    letter-spacing: 0.02em;
  }

  /* Panel */
  .admin-panel {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 1.5rem 3rem;
  }

  /* Topbar */
  .admin-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .admin-topbar h1 {
    font-size: var(--text-xl);
    color: var(--white);
  }

  .admin-topbar h1 i { color: var(--accent-cyan); }

  .admin-topbar-actions {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  /* Buttons */
  .admin-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.5rem 1rem;
    border-radius: var(--radius-sm);
    font-size: var(--text-sm);
    font-weight: 600;
    font-family: var(--font-primary);
    cursor: pointer;
    transition: var(--transition-fast);
    border: 1.5px solid transparent;
    text-decoration: none;
  }

  .admin-btn--primary {
    background: rgba(77, 184, 212, 0.15);
    border-color: rgba(77, 184, 212, 0.4);
    color: var(--accent-cyan);
  }
  .admin-btn--primary:hover {
    background: rgba(77, 184, 212, 0.25);
  }

  .admin-btn--secondary {
    background: rgba(150, 100, 55, 0.15);
    border-color: rgba(150, 100, 55, 0.4);
    color: #d4915a;
  }
  .admin-btn--secondary:hover {
    background: rgba(150, 100, 55, 0.25);
  }

  .admin-btn--outline {
    background: transparent;
    border-color: rgba(45, 130, 170, 0.2);
    color: var(--text-muted);
  }
  .admin-btn--outline:hover {
    border-color: rgba(45, 130, 170, 0.4);
    color: var(--white);
  }

  .admin-btn--sm {
    padding: 0.3rem 0.6rem;
    font-size: var(--text-xs);
  }

  .admin-btn--danger {
    background: rgba(248, 113, 113, 0.1);
    border-color: rgba(248, 113, 113, 0.3);
    color: #f87171;
  }
  .admin-btn--danger:hover {
    background: rgba(248, 113, 113, 0.2);
  }

  /* Tabs */
  .admin-tabs {
    display: flex;
    gap: 0;
    border-bottom: 1px solid rgba(45, 130, 170, 0.1);
    margin-bottom: 1.5rem;
    overflow-x: auto;
  }

  .admin-tab {
    padding: 0.6rem 1rem;
    background: none;
    border: none;
    border-bottom: 2px solid transparent;
    color: var(--text-muted);
    font-size: var(--text-sm);
    font-weight: 600;
    font-family: var(--font-primary);
    cursor: pointer;
    transition: var(--transition-fast);
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .admin-tab:hover { color: var(--white); }
  .admin-tab--active {
    color: var(--accent-cyan);
    border-bottom-color: var(--accent-cyan);
  }

  .admin-tab-content {
    display: none;
  }
  .admin-tab-content--active {
    display: block;
  }

  /* Card */
  .admin-card {
    background: rgba(12, 20, 36, 0.8);
    border: 1px solid rgba(45, 130, 170, 0.1);
    border-radius: var(--radius-md);
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .admin-card h2 {
    font-size: var(--text-lg);
    color: var(--white);
    margin-bottom: 1rem;
  }

  .admin-card h2 i { color: var(--accent-cyan); margin-right: 0.3rem; }

  .admin-subtitle {
    font-size: var(--text-base);
    color: var(--accent-cyan);
    margin: 1.5rem 0 1rem;
  }

  .admin-hint {
    font-size: var(--text-sm);
    color: var(--text-muted);
    margin-bottom: 1rem;
  }

  .admin-hint code {
    background: rgba(77, 184, 212, 0.1);
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
    font-size: 0.8em;
    color: var(--accent-cyan);
  }

  /* Forms */
  .admin-form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .admin-form-group {
    margin-bottom: 1rem;
  }

  .admin-form-group label {
    display: block;
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--text-light);
    margin-bottom: 0.3rem;
  }

  .admin-form-group input,
  .admin-form-group select,
  .admin-form-group textarea {
    width: 100%;
    padding: 0.55rem 0.8rem;
    font-size: var(--text-sm);
    font-family: var(--font-secondary);
    color: var(--text-light);
    background: rgba(10, 18, 32, 0.8);
    border: 1px solid rgba(45, 130, 170, 0.2);
    border-radius: var(--radius-sm);
    transition: var(--transition-fast);
  }

  .admin-form-group input:focus,
  .admin-form-group select:focus,
  .admin-form-group textarea:focus {
    outline: none;
    border-color: var(--accent-cyan);
    box-shadow: 0 0 0 3px rgba(77, 184, 212, 0.1);
  }

  .admin-form-group input[type="file"] {
    padding: 0.4rem;
    cursor: pointer;
  }

  .admin-form-group select option {
    background: #0b1a2e;
    color: var(--text-light);
  }

  .admin-form-actions {
    display: flex;
    gap: 0.75rem;
    margin-top: 1rem;
    flex-wrap: wrap;
  }

  /* ├ëtapes table */
  .admin-etapes-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9rem;
  }

  .admin-etapes-table thead th {
    padding: 8px 10px;
    text-align: left;
    color: var(--accent-cyan);
    border-bottom: 2px solid rgba(77, 184, 212, 0.25);
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
  }

  .admin-etapes-table tbody td {
    padding: 6px 10px;
    border-bottom: 1px solid rgba(77, 184, 212, 0.06);
    vertical-align: middle;
  }

  .admin-etapes-table tbody tr:hover {
    background: rgba(77, 184, 212, 0.06);
  }



  .admin-etape-editor {
    background: rgba(20, 32, 56, 0.7);
    border: 1px solid rgba(77, 184, 212, 0.2);
    border-radius: var(--radius-md);
    padding: 1.5rem;
    margin-top: 1rem;
  }

  .admin-etape-editor h3 {
    font-size: var(--text-base);
    color: var(--accent-cyan);
    margin-bottom: 1rem;
  }

  /* Journal items */
  .admin-journal-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .admin-journal-item {
    background: rgba(20, 32, 56, 0.5);
    border: 1px solid rgba(45, 130, 170, 0.08);
    border-radius: var(--radius-sm);
    padding: 1rem;
  }

  .admin-journal-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
  }

  .admin-journal-head strong {
    font-size: var(--text-sm);
    color: var(--accent-cyan);
  }

  .admin-journal-item h4 {
    font-size: var(--text-base);
    color: var(--white);
    margin-bottom: 0.35rem;
  }

  .admin-journal-item p {
    font-size: var(--text-sm);
    color: var(--text-muted);
    line-height: 1.6;
  }

  .admin-journal-tags {
    display: flex;
    gap: 0.4rem;
    flex-wrap: wrap;
    margin-top: 0.5rem;
  }

  .admin-tag {
    font-size: 0.65rem;
    padding: 0.15rem 0.5rem;
    border-radius: var(--radius-full);
    background: rgba(77, 184, 212, 0.08);
    border: 1px solid rgba(77, 184, 212, 0.2);
    color: var(--accent-cyan);
  }

  /* Photos grid */
  .admin-photos-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .admin-photo-item {
    position: relative;
    border-radius: var(--radius-sm);
    overflow: hidden;
    aspect-ratio: 4/3;
    background: rgba(20, 32, 56, 0.5);
    border: 1px solid rgba(45, 130, 170, 0.08);
  }

  .admin-photo-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .admin-photo-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4rem 0.5rem;
    background: rgba(0, 0, 0, 0.7);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.3rem;
  }

  .admin-photo-overlay code {
    font-size: 0.6rem;
    color: var(--text-muted);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .admin-photo-preview {
    display: flex;
    gap: 1rem;
    align-items: center;
    margin-bottom: 1rem;
    padding: 0.75rem;
    background: rgba(20, 32, 56, 0.5);
    border: 1px solid rgba(45, 130, 170, 0.08);
    border-radius: var(--radius-sm);
  }

  .admin-photo-preview img {
    width: 120px;
    height: 90px;
    object-fit: cover;
    border-radius: var(--radius-sm);
  }

  .admin-photo-preview-info p {
    font-size: var(--text-sm);
    color: var(--text-muted);
    margin: 0.2rem 0;
  }

  .admin-photo-preview-info code {
    color: var(--accent-cyan);
    font-size: 0.8em;
  }

  /* GPX */
  .admin-gpx-map {
    height: 400px;
    border-radius: var(--radius-md);
    border: 1px solid rgba(45, 130, 170, 0.12);
    overflow: hidden;
    margin: 1rem 0;
    background: var(--primary-dark);
  }

  .admin-gpx-info {
    background: rgba(20, 32, 56, 0.5);
    border: 1px solid rgba(45, 130, 170, 0.08);
    border-radius: var(--radius-sm);
    padding: 1rem;
    margin: 1rem 0;
  }

  .admin-gpx-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .admin-gpx-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.6rem 0.8rem;
    background: rgba(20, 32, 56, 0.5);
    border: 1px solid rgba(45, 130, 170, 0.08);
    border-radius: var(--radius-sm);
  }

  .admin-gpx-item-info strong {
    display: block;
    font-size: var(--text-sm);
    color: var(--white);
  }

  .admin-gpx-item-info strong i {
    color: var(--accent-cyan);
    margin-right: 0.3rem;
  }

  .admin-gpx-item-info span {
    font-size: var(--text-xs);
    color: var(--text-muted);
  }

  /* JSON Preview */
  .admin-json-preview {
    background: rgba(10, 18, 32, 0.9);
    border: 1px solid rgba(45, 130, 170, 0.1);
    border-radius: var(--radius-sm);
    padding: 1rem;
    font-size: 0.72rem;
    color: var(--accent-cyan);
    overflow-x: auto;
    max-height: 500px;
    overflow-y: auto;
    white-space: pre;
    font-family: 'Cascadia Code', 'Fira Code', monospace;
    line-height: 1.5;
  }

  /* Toast */
  .admin-toast {
    position: fixed;
    bottom: 60px;
    left: 50%;
    transform: translateX(-50%) translateY(20px);
    background: rgba(12, 20, 36, 0.95);
    border: 1px solid rgba(77, 184, 212, 0.3);
    color: var(--accent-cyan);
    padding: 0.6rem 1.5rem;
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: 600;
    z-index: 9999;
    opacity: 0;
    transition: all 0.3s ease;
    pointer-events: none;
  }

  .admin-toast--show {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }

  /* Light theme */
  :global([data-theme="light"]) .admin-auth-card,
  :global([data-theme="light"]) .admin-card,
  :global([data-theme="light"]) .admin-journal-item,
  :global([data-theme="light"]) .admin-gpx-item,
  :global([data-theme="light"]) .admin-gpx-info,
  :global([data-theme="light"]) .admin-etape-editor {
    background: rgba(255, 255, 255, 0.85);
    border-color: rgba(10, 24, 44, 0.1);
  }

  :global([data-theme="light"]) .admin-etapes-table tbody tr:hover {
    background: rgba(77, 184, 212, 0.08);
  }

  :global([data-theme="light"]) .admin-form-group input,
  :global([data-theme="light"]) .admin-form-group select,
  :global([data-theme="light"]) .admin-form-group textarea {
    background: rgba(255, 255, 255, 0.9);
    border-color: rgba(10, 24, 44, 0.15);
    color: #1a2535;
  }

  :global([data-theme="light"]) .admin-toast {
    background: rgba(255, 255, 255, 0.95);
    border-color: rgba(26, 127, 160, 0.3);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .admin-form-row {
      grid-template-columns: 1fr;
    }
    .admin-topbar {
      flex-direction: column;
      align-items: flex-start;
    }
    .admin-topbar-actions {
      width: 100%;
    }
    .admin-tabs {
      gap: 0;
    }
    .admin-tab {
      padding: 0.5rem 0.65rem;
      font-size: var(--text-xs);
    }
  }
</style>
